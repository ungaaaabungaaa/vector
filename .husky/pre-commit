#!/usr/bin/env sh

# Run lint-staged on added/copied/modified files only.
# Excluding rename entries avoids git path resolution errors on symlink migrations.
pnpm exec lint-staged --concurrent false --diff-filter=ACM
